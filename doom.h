#ifndef DOOM_H
#define DOOM_H

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

/* Shell Functions */
int p_text(char *text);
char **str_to_2d(char *buff_str);

/* Basic Functions */
int _putchar(char c);

/* Strings Functions */
char *_strdup(char *str);

#endif /* DOOM_H */
